apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ig.fullname" . }}-test-connection"
  labels:
    {{- include "ig.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
