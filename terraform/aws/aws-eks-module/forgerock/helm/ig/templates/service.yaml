apiVersion: v1
kind: Service
metadata:
  name: {{ include "ig.fullname" . }}
  labels:
    {{- include "ig.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ig.selectorLabels" . | nindent 4 }}
