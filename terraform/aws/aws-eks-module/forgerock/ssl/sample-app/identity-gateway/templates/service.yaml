apiVersion: v1
kind: Service
metadata:
  name: {{ include "identity-gateway.fullname" . }}
  labels:
    {{- include "identity-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "identity-gateway.selectorLabels" . | nindent 4 }}
